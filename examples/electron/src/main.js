/**
 * Pulse Notes - Electron Desktop App
 * Entry point - imports and mounts the compiled App component
 */

import App from './App.pulse';

// Mount the application
App.mount('#app');

console.log('Pulse Notes (Electron) loaded!');
